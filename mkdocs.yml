site_name: Libby
site_description: A multi-agent decision-support framework for identifying candidate therapeutics for an individual cancer patient. Not medical advice.
site_url: https://benjamingvincent.github.io/libby/
repo_url: https://github.com/BenjaminGVincent/libby
repo_name: BenjaminGVincent/libby
edit_uri: edit/main/docs/

theme:
  name: material
  features:
    - navigation.top
    - navigation.instant
    - navigation.tracking
    - search.highlight
    - search.suggest
    - content.code.copy
    - content.action.edit
    - content.tables.sortable
  palette:
    - media: "(prefers-color-scheme: light)"
      scheme: default
      primary: deep purple
      accent: amber
      toggle:
        icon: material/weather-night
        name: Switch to dark mode
    - media: "(prefers-color-scheme: dark)"
      scheme: slate
      primary: deep purple
      accent: amber
      toggle:
        icon: material/weather-sunny
        name: Switch to light mode

markdown_extensions:
  - admonition
  - footnotes
  - tables
  - attr_list
  - md_in_html
  - toc:
      permalink: true
  - pymdownx.superfences
  - pymdownx.highlight:
      anchor_linenums: true
  - pymdownx.inlinehilite
  - pymdownx.details
  - pymdownx.tabbed:
      alternate_style: true

extra_css:
  - stylesheets/trial-table.css
  - stylesheets/libby.css

# Append `?v=<content-hash>` cache-busters to extra_css / extra_javascript
# links so CSS / JS edits ship with a fresh URL and the browser is forced to
# re-fetch instead of serving the prior version from disk cache. Same pattern
# the case-page download links already use for PDFs / HTML artifacts.
hooks:
  - scripts/mkdocs_hooks.py

nav:
  - Home: index.md
  - Cases: cases/index.md
  - Methods: methods.md
  - PHI policy: phi_policy.md
